node_modules
.next/
.claude/
tsconfig.tsbuildinfo
*.mp4
*.mp3
*.mov
*.avi

# arquivos-fonte dos projetos (originais pesados; versoes otimizadas em public/portfolio)
projects/

# entrega do prototipo do site institucional + fotos brutas do Felipe
# (material de referencia; o site portado vive em app/(institucional))
Site institucional para Felipe/
Site institucional para Felipe.zip
fotos Felipe/

# mapas-fonte do metodo PSIQUE (originais 2600px; otimizados em
# public/institucional/mapas). Ancorado na raiz p/ nao pegar o public.
/mapas/

# variaveis de ambiente (credenciais SMTP do form da imersao)
.env
.env.local
.env*.local
.vercel
.env*
